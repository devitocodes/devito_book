# Python dependencies for building the FDM Book
#
# Recommended: Use `pip install -e .` instead, which reads from pyproject.toml
#
# This file is maintained for compatibility with workflows that expect
# requirements.txt. It contains the core dependencies needed to build the book.

# Scientific Python stack (used in examples)
matplotlib>=3.7.0
numpy>=1.24.0
scipy>=1.10.0
sympy>=1.12
